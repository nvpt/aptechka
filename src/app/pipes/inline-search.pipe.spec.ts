import { InlineSearchPipe } from './inline-search.pipe';

describe('InlineSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InlineSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
